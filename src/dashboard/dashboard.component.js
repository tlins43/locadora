'use strict';

// Register `phoneList` component, along with its associated controller and template
angular.
  module('dashboard').
  component('dashboard', {
    templateUrl: 'dashboard/dashboard.component.html',
    StyleSheet: 'dashboard/dashboard.component.css',
    
  });
